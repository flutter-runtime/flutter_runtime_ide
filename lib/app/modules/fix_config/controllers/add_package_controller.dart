import 'package:get/get.dart';

import '../../../data/package_config.dart';

class AddPackageController extends GetxController {
  // List<PackageInfo> packages = [];
  var currentPackageInfo = Rx<PackageInfo?>(null);
}
